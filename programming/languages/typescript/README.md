# Introduction

This folder contains typescript-specific and agonostic code snippets

# To run

1. Ensure that you have `npm` and `node` installed.
1. `npm i`
1. As each file as a main, you can directly run it. `npx ts-node ./src/filename.ts` _(e.g. `npx ts-node src/bind.ts`)_
